# Python dependencies for the export path (tools/export_vit_gguf.py).
#
# The C engine needs none of this. It is here because the exporter loads the
# real checkpoint with torch, which is what buys the ability to PROBE the
# architecture instead of assuming it (see docs / lesson 6) — and that
# capability costs a dependency tree worth pinning.
#
# Exact versions, captured 2026-09-07 from a working .venv on macOS arm64
# (~600 MB total; the 2-3 GB figure people quote is a Linux/CUDA build).
#
#     python3 -m venv .venv
#     .venv/bin/pip install -r requirements.txt
#
# ⚠️ NOT YET HASH-PINNED. Exact versions stop the common case — a dependency
# silently moving to a newly-published malicious release — but they do not
# detect a re-uploaded artifact at the same version. Closing that needs
# `--require-hashes`, which needs the hashes generated at install time:
#
#     pip install pip-tools
#     pip-compile --generate-hashes --output-file requirements.lock requirements.txt
#
# Left undone deliberately rather than faked: writing hashes I derived from an
# already-installed tree would attest to nothing.

anyio==4.15.1
certifi==2026.7.22
click==8.5.0
exceptiongroup==1.3.1
filelock==3.32.5
fsspec==2026.7.0
ftfy==6.3.1
h11==0.16.0
hf-xet==1.6.0
httpcore==1.0.9
httpx==0.28.1
huggingface_hub==1.30.0
idna==3.19
Jinja2==3.1.6
MarkupSafe==3.0.3
mpmath==1.3.0
networkx==3.4.2
numpy==2.2.6
open_clip_torch==3.3.0
packaging==26.3
pillow==12.3.0
PyYAML==6.0.3
regex==2026.9.3
safetensors==0.8.0
sympy==1.14.0
timm==1.0.29
torch==2.14.0
torchvision==0.29.0
tqdm==4.70.0
typing_extensions==4.16.0
wcwidth==0.8.3
