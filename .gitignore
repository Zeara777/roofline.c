build/
*.o
*.d
*.gguf
# ...but the golden vectors are the op gate and must travel with the repo.
# Regenerating them on a different numpy build could shift the reference.
!tests/fixtures/*.gguf
*.bin
.DS_Store
compile_commands.json
.cache/
.venv/

# Personal study material. divegomobile keeps its equivalent in
# .git/info/exclude; this one is in .gitignore instead because roofline.c is
# public and cloned to more than one machine — the rule should travel.
StdyDocs/
